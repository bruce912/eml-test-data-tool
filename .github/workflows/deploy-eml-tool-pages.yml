name: Deploy EML Tool to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'eml-test-data-tool/**'
      - '.github/workflows/deploy-eml-tool-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: eml-test-data-tool/package-lock.json
      - name: Install dependencies
        working-directory: eml-test-data-tool
        run: npm ci
      - name: Test
        working-directory: eml-test-data-tool
        run: npm test
      - name: Build
        working-directory: eml-test-data-tool
        run: npm run build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: eml-test-data-tool/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
